package com.mcpserver.connectors;

/** SHAREPOINT is reserved for a future connector — not implemented yet (no Graph/Entra tenant available). */
public enum ConnectionType {
    CONFLUENCE,
    JIRA,
    SHAREPOINT,
    API_COLLECTION
}
